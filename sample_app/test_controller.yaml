project_type: flutter

dependencies:
  - name: flutter
    min_version: "3.29.4"
    version_command: flutter --version | head -n 1 | awk '{print $2}'

hooks:
  pre_run:
    - command: "echo"
      args: ["Pre-run: could start Playserve here"]
